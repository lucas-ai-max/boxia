// Substituído por @/components/ui — re-exports para evitar quebrar imports antigos.
export { Card as Box, Chip as Pill, Button as Btn, ScoreRing as ScoreDot, Avatar } from './ui';
export const Squiggle = () => null;
